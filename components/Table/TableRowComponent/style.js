import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container_three: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
  },
  
  rowContainer: {
    flexDirection: "row",
    padding: 15,
    borderBottomWidth: 1,
    borderBottomColor: "#f1f1f1",
    alignItems: "center",
  },

  rowText: {
    flex: 1,
    textAlign: "center",
    color: "#555",
  },
});