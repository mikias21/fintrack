import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },

  container_one_main: {
    width: "100%",
    backgroundColor: "#fff",
  },

  container_one: {
    paddingHorizontal: 18,
    paddingTop: 18,
    paddingBottom: 10,
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    marginTop: 10,
  },
  
  header_image: {
    height: 40,
    width: 40,
    marginRight: 10,
  },

  text_one: {
    fontSize: 24,
    fontWeight: "bold",
  },
});
