import { StyleSheet} from "react-native";

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },

  container_main_one: {
    width: "100%",
    backgroundColor: "#fff",
  },

  container_one: {
    padding: 18,
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    marginTop: 10,
  },

  container_two: {
    marginBottom: 10,
  },

  header_image: {
    height: 40,
    width: 40,
    marginRight: 10,
  },

  text_one: {
    fontSize: 24,
    fontWeight: "bold",
  },

  text_two_one: {
    padding: 18,
    fontSize: 24,
    fontWeight: "bold",
  },

  text_two: {
    fontSize: 24,
    fontWeight: "bold",
    paddingLeft: 18,
    paddingBottom: 3,
  },

  final_list: {
    marginBottom: 20,
  }
});
