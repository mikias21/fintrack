import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#fff",
        
    },

    subcontainer: {
        alignItems: "center",
        marginTop: 150,
    },

    image: {
        height: 100,
        width: 100,
    }
});

export default styles;